<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Cryptoapp!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <!-- TODO: style with bootstrap -->
      <div class="row">
        <%= if @current_user do %>
          <div class="col-9">
            <p class="p-4">
              <a href="/alerts">Subscriptions</a> |
              <a href="/users">All Users</a>
            </p>
          </div>
          <div class="col-3">
            <p class="p-4">
              Logged in as: <%= @current_user.name %> |
              <%= link "Log Out", to: "/session", method: :delete %>
            </p>
          </div>
        <% else %>
            <div class="col-3 offset-9">
              <p>Not logged in.</p>
            </div>
        <% end %>
      </div>

      <div class="row">
        <div class="col-12">
          <%= if @current_user do %>
            <div class="header">
              <h1>Cryptocurrency Monitoring System</h1>
            </div>
            <div class="topnav">
                <%= link "Subscriptions", to: alert_path(@conn, :index) %>
                <%= link "Users", to: user_path(@conn, :index) %>
            </div>
          <% else %>
            <div class="header">
              <h1>Cryptocurrency Monitoring System</h1>
            </div>
            <div class="topnav">
              <%= link "Sign In", to: page_path(@conn, :index) %>
            </div>
          <% end %>
        </div>
      </div>

      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>

      <main>
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
